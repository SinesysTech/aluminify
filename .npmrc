# Silenciar mensagens de funding
fund=false

# Configurar nível de auditoria (mostrar apenas vulnerabilidades críticas)
audit-level=high

# Reduzir verbosidade de warnings de deprecated
loglevel=warn

# Ignorar conflitos de peer dependencies (necessário para @ag-ui/mastra + zod v4)
legacy-peer-deps=true
